Index: b/src/util.cpp
===================================================================
--- a/src/util.cpp
+++ b/src/util.cpp
@@ -1745,8 +1745,6 @@ nextChar:
           (s.at(i-1)==')' && isId(c)) // ")id" -> ") id"
           ||
           (c=='\''  && s.at(i-1)==' ')  // "'id" -> "' id"
-          ||
-          (i>1 && s.at(i-2)==' ' && s.at(i-1)==' ') // "  id" -> " id"
          )
         )
     {
